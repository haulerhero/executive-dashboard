FROM python:3.11-slim

   WORKDIR /app

   # Install dependencies
   COPY extraction/requirements.txt .
   RUN pip install --no-cache-dir -r requirements.txt

   # Copy extraction script
   COPY extraction/hubspot_pull.py .

   # Set entrypoint
   CMD ["python", "hubspot_pull.py"]